name: DevOps Pipeline running inside a Docker Container

trigger:
- main

stages:
- stage: build

  pool:
   name: linux-containers-aks
   # vmImage: ubuntu-latest

  jobs:
  - job: buildApp

    strategy:
      matrix:
        build01:
          PYTHON_VERSION: '3.5'
        build02:
          PYTHON_VERSION: '3.6'
        build03:
          PYTHON_VERSION: '3.7'
        build04:
          PYTHON_VERSION: '3.6'
        build05:
          PYTHON_VERSION: '3.7'
        build06:
          PYTHON_VERSION: '3.7'
      maxParallel: 7

    steps:
    - script: |
        echo "Hello from inside Docker Container: $HOSTNAME"
        printenv

  # - job: buildApp02
  #   steps:
  #   - script: |
  #       echo "Hello from inside Docker Container: $HOSTNAME"
  #       printenv

  # - job: buildApp03
  #   steps:
  #   - script: |
  #       echo "Hello from inside Docker Container: $HOSTNAME"
  #       printenv
